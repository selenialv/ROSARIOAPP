@model ROSARIOAPP.Models.Nota_Matricula

@{
    ViewData["Title"] = "Details";
    Layout = null;
}

<h1>Details</h1>

<div>
    <h4>Nota_Matricula</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nota)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nota.Idnota)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Matricula)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Matricula.Idmatricula)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*@if (Model.Nota.Count == 0)
{
    <h7> Este grupo no tiene asignación.</h7>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    Estudiante
                </th>
                <th>

                    @Html.DisplayNameFor(model => model.Nota.FirstOrDefault().Parcial)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.corte_evaluativo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.nota_final)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IdmateriaNavigation)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Nota)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nota_Matricula)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.parcial)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.corte_evaluativo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.nota_final)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdmateriaNavigation.Idmateria)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Idnota">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Idnota">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Idnota">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}*@